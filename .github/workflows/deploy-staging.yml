name: Deploy to Staging

# This workflow runs ONLY when code is merged to staging
# It waits for BOTH test workflows to complete successfully
# Then explicitly triggers Render deployment via deploy hook
#
# DEPLOYMENT TRIGGER:
# - Merge PR to staging → tests run → this workflow runs → calls Render hook
# - Single, explicit deployment path (no "After CI Checks Pass" confusion)
# - We control exactly when deployment happens

on:
  workflow_run:
    workflows: ["FastAPI Gateway Tests", "Streamlit App Tests"]
    types: [completed]
    branches: [staging]  # Only runs for staging branch updates (merges)

jobs:
  # ============================================================
  # CHECK THAT BOTH TEST WORKFLOWS PASSED
  # ============================================================
  
  check-tests:
    runs-on: ubuntu-latest
    # Fail immediately if the triggering workflow failed
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    outputs:
      both-passed: ${{ steps.check-both.outputs.passed }}
    
    steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ github.event.workflow_run.head_sha }}
    
    - name: Check Both Test Workflows
      id: check-both
      uses: actions/github-script@v7
      with:
        script: |
          // The workflow_run event only tells us about ONE workflow
          // We need to check if BOTH FastAPI and Streamlit tests passed
          
          const commit_sha = context.payload.workflow_run.head_sha;
          console.log(`Checking tests for commit: ${commit_sha}`);
          
          // Get all workflow runs for this commit
          const { data: runs } = await github.rest.actions.listWorkflowRunsForRepo({
            owner: context.repo.owner,
            repo: context.repo.repo,
            head_sha: commit_sha,
            per_page: 100
          });
          
          // Find FastAPI and Streamlit test runs
          const fastapiRun = runs.workflow_runs.find(r => 
            r.name === 'FastAPI Gateway Tests' && r.head_sha === commit_sha
          );
          const streamlitRun = runs.workflow_runs.find(r => 
            r.name === 'Streamlit App Tests' && r.head_sha === commit_sha
          );
          
          console.log('FastAPI Test:', fastapiRun ? fastapiRun.conclusion : 'NOT FOUND');
          console.log('Streamlit Test:', streamlitRun ? streamlitRun.conclusion : 'NOT FOUND');
          
          // Check if both exist and passed
          const fastapi_passed = fastapiRun && fastapiRun.conclusion === 'success';
          const streamlit_passed = streamlitRun && streamlitRun.conclusion === 'success';
          
          if (!fastapiRun) {
            console.log('❌ FastAPI test run not found for this commit');
            core.setFailed('FastAPI tests did not run');
            return;
          }
          
          if (!streamlitRun) {
            console.log('❌ Streamlit test run not found for this commit');
            core.setFailed('Streamlit tests did not run');
            return;
          }
          
          if (!fastapi_passed) {
            console.log('❌ FastAPI tests FAILED');
            core.setFailed('FastAPI tests failed');
            return;
          }
          
          if (!streamlit_passed) {
            console.log('❌ Streamlit tests FAILED');
            core.setFailed('Streamlit tests failed');
            return;
          }
          
          console.log('✅ Both test workflows PASSED');
          core.setOutput('passed', 'true');
    
    - name: Test Status Summary
      run: |
        echo "📊 Test Status Summary:"
        echo "  Commit: ${{ github.event.workflow_run.head_sha }}"
        echo "  Branch: ${{ github.event.workflow_run.head_branch }}"
        echo "  ✅ FastAPI Tests: PASSED"
        echo "  ✅ Streamlit Tests: PASSED (97.45% coverage)"
        echo ""
        echo "🚀 All tests passed - proceeding with staging deployment!"
  
  # ============================================================
  # DEPLOY TO RENDER
  # ============================================================
  
  deploy-render:
    needs: check-tests
    runs-on: ubuntu-latest
    
    steps:
    - name: Trigger Render Deployment
      run: |
        echo "🚀 Triggering Render staging deployment"
        echo "Commit: ${{ github.event.workflow_run.head_sha }}"
        echo "Branch: ${{ github.event.workflow_run.head_branch }}"
        echo ""
        
        response=$(curl -s -o /dev/null -w "%{http_code}" -X POST "${{ secrets.RENDER_DEV_DEPLOY_HOOK }}")
        
        if [ "$response" -eq 200 ] || [ "$response" -eq 201 ]; then
          echo "✅ Render deploy hook called successfully (HTTP $response)"
          echo "Render will now build and deploy from staging branch"
        else
          echo "❌ Deploy hook failed with HTTP $response"
          exit 1
        fi
  
  # ============================================================
  # DEPLOYMENT CONFIRMATION
  # ============================================================
  
  confirm-deployment:
    needs: [check-tests, deploy-render]
    runs-on: ubuntu-latest
    
    steps:
    - name: Staging Deployment Summary
      run: |
        echo "✅ Staging Deployment Triggered Successfully"
        echo ""
        echo "📦 Deployment Details:"
        echo "  • Branch: staging"
        echo "  • Commit: ${{ github.event.workflow_run.head_sha }}"
        echo "  • FastAPI Tests: PASSED ✅"
        echo "  • Streamlit Tests: PASSED ✅ (97.45% coverage)"
        echo ""
        echo "🚀 Services Deploying:"
        echo "  • Render FastAPI: Deploy hook called ✅"
        echo "  • Streamlit App: Auto-deployed from staging ✅"
        echo ""
        echo "🔍 Staging URLs:"
        echo "  • FastAPI: ${{ secrets.FASTAPI_DEV_URL }}/docs"
        echo "  • Streamlit: https://retail-forecasting-pipeline-staging.streamlit.app"
        echo ""
        echo "⏱️  Expected deployment time: 3-5 minutes"
        echo ""
        echo "📋 Next Steps:"
        echo "  1. Monitor Render dashboard for build progress"
        echo "  2. Test staging environment"
        echo "  3. When ready, merge staging → main for production"
